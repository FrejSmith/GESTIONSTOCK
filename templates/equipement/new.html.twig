{% extends 'base.html.twig' %}

{% block title %}Ajouter un équipement{% endblock %}

{% block body %}
    <h1>Ajouter un équipement</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}
    <a href="{{ path('equipement_index') }}">Retour à la liste</a>
{% endblock %}
