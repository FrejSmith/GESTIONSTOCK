<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="{{ asset('css/style.css') }}">
</head>
<body>
    <h1>{{ title }}</h1>
    <div id="inventory-table">
        <form action="{{ path('inventory_save') }}" method="POST">
            <table class="inventory">
                <thead>
                    <tr>
                        <th>Produit</th>
                        <th>Quantité</th>
                        <th>Prix</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td><input type="text" name="product_name" placeholder="Nom du produit" required></td>
                        <td><input type="number" name="quantity" placeholder="Quantité" required></td>
                        <td><input type="number" step="0.01" name="price" placeholder="Prix (€)" required></td>
                    </tr>
                </tbody>
            </table>
            <button type="submit">Enregistrer</button>
        </form>
    </div>
    <script src="{{ asset('js/script.js') }}"></script>
</body>
</html>