{% extends 'base.html.twig' %}

{% block title %}Contactez-nous{% endblock %}

{% block body %}
<section id="contact-section" style="background:#f8fafc; padding:60px 0;">
  <div style="max-width:700px; margin:0 auto; padding:0 20px;">
    <h2 style="font-size:2rem; font-weight:bold; color:#1a2236; text-align:center; margin-bottom:32px;">
      Contactez-nous
    </h2>
    <div style="background:#fff; border-radius:14px; box-shadow:0 2px 8px 0 rgba(30,40,60,0.07); padding:32px 24px;">
      <p style="font-size:1.1rem; color:#556080; margin-bottom:18px;">
        Pour toute question ou demande, contactez-nous par email ou via le formulaire ci-dessous.
      </p>
      <p style="margin-bottom:24px;">
        <i class="fas fa-envelope" style="color:#377dff; margin-right:8px;"></i>
        <a href="mailto:contact@gestionstock.com" style="color:#377dff; font-weight:600;">contact@gestionstock.com</a>
      </p>
      {% if error %}
        <div style="color:#e94f4f; font-weight:bold; margin-bottom:18px;">{{ error }}</div>
      {% endif %}
      {% if success %}
        <div style="color:#44c97f; font-weight:bold; margin-bottom:18px;">Votre message a bien été envoyé !</div>
      {% endif %}
      <form method="post" action="{{ path('contact') }}">
        <div style="margin-bottom:16px;">
          <input type="text" name="nom" placeholder="Votre nom" required style="width:100%;padding:10px;border-radius:6px;border:1px solid #e0e0e0;">
        </div>
        <div style="margin-bottom:16px;">
          <input type="email" name="email" placeholder="Votre email" required style="width:100%;padding:10px;border-radius:6px;border:1px solid #e0e0e0;">
        </div>
        <div style="margin-bottom:16px;">
          <textarea name="message" placeholder="Votre message" rows="5" required style="width:100%;padding:10px;border-radius:6px;border:1px solid #e0e0e0;"></textarea>
        </div>
        <button type="submit" style="background:#377dff;color:#fff;border:none;border-radius:8px;padding:10px 28px;font-size:1rem;font-weight:500;cursor:pointer;">Envoyer</button>
      </form>
    </div>
  </div>
</section>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
{% endblock %}